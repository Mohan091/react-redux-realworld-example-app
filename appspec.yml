version: 0.0
os: linux
files:  
  - source: /
    destination: /home/ubuntu
hooks:
  BeforeInstall: 
    - location: Scripts/Install_dependencies.sh
      timeout: 300
      runas: root
  Install: 
    - runas: root     
  AfterInstall: 
    - location: Scripts/After_Install.sh 
      timeout: 300
      runas: root